1\. **Create a virtual environment**: myenv\\Scripts\\activate

2\. **Open myproject and work on it**: django-admin startproject myproject

3**\*\*. go to myproject\*\***: cd myproject

4\. **run the server**: python manage.py runserver

5\. **check the link**: http://127.0.0.1:8000/?utm\_source=chatgpt.com

6\. **create an app**: python manage.py startapp myapp

7\. **Inside myapp/, create a new file called urls.py.**

8\. **Add This Code to myapp/urls.py:**

from django.urls import path

from . import views



urlpatterns = \[

&nbsp;   path('', views.home, name='home'),  # root URL → calls home view

]

&nbsp; 

**9.Add a View in views.py:**

&nbsp;  from django.http import HttpResponse



def home(request):

&nbsp;   return HttpResponse("Hello from myapp!")

**10.Open myproject/urls.py (the one next to settings.py)**

from django.contrib import admin

from django.urls import path, include



urlpatterns = \[

&nbsp;   path('admin/', admin.site.urls),

&nbsp;   path('', include('myapp.urls')),  # connects app urls

]

**11.Run Server**:python manage.py runserver(http://127.0.0.1:8000/?utm\_source=chatgpt.com)







